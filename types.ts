export type SliderType = {
  sliderId: string;
  sliderIcon: string;
  sliderText: string;
};

export type FooterType = {
  footerId: string;
  footerIcon: string;
  footerHeading: string;
  footerDescription: string;
};
